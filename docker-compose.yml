services:
  worldcup-tracker:
    build: .
    container_name: worldcup-tracker
    restart: unless-stopped
    environment:
      FOOTBALL_DATA_TOKEN: ${FOOTBALL_DATA_TOKEN:-}
      PORT: ${PORT:-4326}
    ports:
      - "${HOST_PORT:-4326}:${PORT:-4326}"
